 kubectl get secret route53-credentials -n cert-manager -o yaml |
 \\n  sed 's/namespace: cert-manager/namespace: external-dns/' | 
 \\n  kubectl apply -f -

 kubectl get secret wildcard-tls-prod -n cert-manager -o yaml |
 sed 's/namespace: cert-manager/namespace: sonarr/' | 
 kubectl apply -f -


Take these images and convert them to json. 

Use this json format:
{
  "source": "Dune: Imperium Card Database",
  "card_name": "Card Name Here",
  "card_type": "Character/Action/etc",
  "game": "Dune: Imperium",
  "cost": 0,
  "description": "...",
  "keywords": ["..."],
  "lore_summary": "...",
  "related_concepts": ["..."]
}

Do not output anything other than the json.
